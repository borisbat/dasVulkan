name: Integration tests

# Headless offscreen Vulkan suite for dasVulkan. Unlike the dasImgui-family
# packages, these tests run fully IN-PROCESS (offscreen render to an image +
# pixel readback; compute to a storage buffer) -- no window, no live subprocess.
# CI renders on Mesa lavapipe (the llvmpipe software Vulkan ICD), so it needs no
# GPU. The package vendors volk + Vulkan-Headers and the generated bindings are
# committed, so the build needs no Vulkan SDK.
#
# Linux-first (lavapipe is the most reliable software Vulkan in CI). Windows
# (lavapipe/swiftshader via jakoch/install-vulkan-sdk-action) is a follow-on;
# macOS headless Vulkan is impractical and skipped.
on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: tests-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  integration:
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: "true"
    defaults:
      run:
        shell: bash
    steps:
      - name: "Checkout daslang"
        uses: actions/checkout@v4
        with:
          repository: GaijinEntertainment/daScript
          path: daslang-src
          ref: master

      - name: "Checkout dasVulkan"
        uses: actions/checkout@v4
        with:
          path: dasVulkan

      - name: "Install lavapipe (software Vulkan) + tools"
        run: |
          sudo apt-get update
          sudo apt-get install -y mesa-vulkan-drivers libvulkan1 vulkan-tools

      - name: "Install CMake and Ninja"
        uses: lukka/get-cmake@latest

      - name: "Set up sccache"
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: "Build daslang"
        working-directory: daslang-src
        # daspkg itself requires the pugixml module (utils/daspkg/commands.das ->
        # PUGIXML_boost), so build it. LLVM/HV/GLFW are not needed -- the dasVulkan
        # generated C++ is committed and the module only links volk + vendored headers.
        run: |
          set -eux
          cmake --no-warn-unused-cli -B./build \
            -DCMAKE_BUILD_TYPE:STRING=Release \
            -DDAS_HV_DISABLED=ON \
            -DDAS_PUGIXML_DISABLED=OFF \
            -DDAS_LLVM_DISABLED=ON \
            -DDAS_GLFW_DISABLED=ON \
            -DCMAKE_C_COMPILER_LAUNCHER=sccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=sccache \
            -G Ninja
          cmake --build ./build --parallel --target daslang dasModulePUGIXML
          sccache --show-stats
          echo "BIN_DIR=bin" >> "$GITHUB_ENV"

      - name: "daspkg install dasVulkan (global, builds the native module)"
        working-directory: daslang-src
        run: |
          set -eux
          ${BIN_DIR}/daslang utils/daspkg/main.das -- install ../dasVulkan --global

      - name: "Verify lavapipe is visible to the loader"
        env:
          VK_DRIVER_FILES: /usr/share/vulkan/icd.d/lvp_icd.x86_64.json
        run: vulkaninfo --summary | grep -iE "deviceName|llvmpipe|apiVersion" || true

      - name: "Run integration tests (offscreen, lavapipe)"
        working-directory: ${{ github.workspace }}/daslang-src
        # VK_DRIVER_FILES points the loader at lavapipe. MUST NOT run under sudo --
        # the Vulkan loader ignores VK_DRIVER_FILES for elevated processes.
        env:
          VK_DRIVER_FILES: /usr/share/vulkan/icd.d/lvp_icd.x86_64.json
        run: |
          set -eux
          ${BIN_DIR}/daslang dastest/dastest.das -- \
            --test modules/dasVulkan/tests/integration \
            --timeout 600 \
            --isolated-mode \
            --isolated-mode-threads 4
